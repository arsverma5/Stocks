package oldstocksimulator.oldmodel;


/**
 * ModelImpl implements the IModel interface and contains
 * all the methods. This also has its own list of portfolios,
 * representing the portfolios that the user has currently made,
 * as well as an API caller to retrieve data for each stock.
 * Inside this class, there are two subclasses:
 * - Portfolio
 * - Stock
 */
public class ModelImpl extends AbstractModelImpl {

  public ModelImpl() {
    super();
  }

}


